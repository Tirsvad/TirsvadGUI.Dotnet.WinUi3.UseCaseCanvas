<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TirsvadGUI.ArtefactsGenerator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TirsvadGUI.ArtefactsGenerator"
    xmlns:controls="using:TirsvadGUI.ArtefactsGenerator.Mvvm.Controls"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TirsvadGUI.ArtefactsGenerator">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <controls:AppMenuBarUC Grid.Row="0"/>

        <!-- Main Content Section -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="NavigationPane" Grid.Column="0" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                <!-- Navigation items go here -->
                <Grid Grid.Column="0"></Grid>
                <!-- Content Section -->
                <Frame Grid.Column="1" x:Name="ContentFrame"/>
            </Grid>
        </Grid>
        <!-- Footer Section -->
        <controls:FooterUC  Grid.Row="2"/>
    </Grid>
</Window>
