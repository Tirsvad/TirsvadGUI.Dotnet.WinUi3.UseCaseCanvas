<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="Root"
    x:Class="TirsvadGUI.ArtefactsGenerator.Mvvm.Controls.AppMenuBarUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TirsvadGUI.ArtefactsGenerator.Mvvm.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:TirsvadGUI.ArtefactsGenerator.Mvvm.Controls.ViewModels" 
    d:DataContext="{d:DesignInstance Type=viewmodels:AppMenuBarUCViewModel}"
    mc:Ignorable="d">

    <Grid>
        <MenuBar>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New" />
                <MenuFlyoutItem Text="Open" />
                <MenuFlyoutItem Text="Save" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Command="{Binding ExitApplicationAsyncCommand}" />
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="Documentation" />
                <MenuFlyoutItem Text="About" />
            </MenuBarItem>
        </MenuBar>
    </Grid>
</UserControl>
